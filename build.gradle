group 'com.thetestroom'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

// When building a jar, we need to tell where the 'main' method is
jar {
    manifest {
        attributes(
                'Main-Class': 'Application'
        )
    }
}
